import { FC, SVGProps } from 'react'

import Svg from 'icons/EmailVerificationIcon/svg/email-verification.svg'

export const EmailVerificationIcon: FC<SVGProps<SVGElement>> = (props) => (
  <Svg {...props} />
)
