import { FC, SVGProps } from 'react'

import Svg from 'icons/SessionExtension/svg/extension-icon.svg'

export const SessionExtensionIcon: FC<SVGProps<SVGElement>> = (props) => (
  <Svg {...props} />
)
