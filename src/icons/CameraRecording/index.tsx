import { FC, SVGProps } from 'react'

import Svg from 'icons/CameraRecording/svg/camera_recordingIcon.svg'

export const CameraRecordingIcon: FC<SVGProps<SVGElement>> = (props) => (
  <Svg {...props} />
)
