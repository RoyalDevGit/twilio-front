import styled from '@emotion/styled'

export const CalendarNavigatorContainer = styled.div`
  position: absolute;
  left: 50%;
  transform: translate(-50%);
  display: flex;
  margin: 0 auto;
  align-items: center;
`

export const CalendarNavigatorButton = styled.div`
  display: flex;
  cursor: pointer;
`

export const CalendarNavigatorText = styled.div`
  min-width: 400px;
  text-align: center;
  font-size: 1.4rem;
`
