import styled from '@emotion/styled'

import { Link } from 'components/Link'

export const CTAContainer = styled.span``

export const CTALabel = styled(Link)`
  font-weight: 500;
  font-size: 0.875rem;
  text-decoration: underline;
`
