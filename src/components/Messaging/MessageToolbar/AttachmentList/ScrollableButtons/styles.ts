import styled from '@emotion/styled'

import { ArrowButton } from 'components/Messaging/MessageToolbar/AttachmentList/styles'

export const RightButton = styled(ArrowButton)`
  right: -22px;
`
export const LeftButton = styled(ArrowButton)`
  left: -10px;
`
